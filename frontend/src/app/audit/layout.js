export const metadata = {
  title: 'Audit Trail',
  description: 'Full traceability of every AI agent action, tool call, and decision. Compliance-ready audit logs for insurance claim processing.',
};

export default function AuditLayout({ children }) {
  return children;
}
