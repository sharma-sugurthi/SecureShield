export const metadata = {
  title: 'Upload Policy',
  description: 'Upload your health insurance policy PDF and let AI extract every rule, limit, and exclusion automatically. Supports all major Indian health insurance providers.',
  openGraph: {
    title: 'Upload Policy — PolicyEye',
    description: 'Upload your insurance policy PDF. AI extracts every clause in seconds.',
  },
};

export default function UploadLayout({ children }) {
  return children;
}
