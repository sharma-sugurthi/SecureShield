export const metadata = {
  title: 'Eligibility History',
  description: 'Review all your past health insurance eligibility checks, verdicts, and coverage breakdowns in one place.',
};

export default function HistoryLayout({ children }) {
  return children;
}
